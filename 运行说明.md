# 当前系统运行说明

## 1. 文档用途

本文档用于记录当前项目在竞赛化改造开始前的基线状态，帮助后续开发者快速理解：

- 项目如何启动
- 前后端分别运行在什么端口
- 当前已有哪些能力
- 当前可用于验证基线是否正常的接口有哪些

本文档只描述现状，不包含竞赛化改造后的新增设计。

## 2. 当前项目结构概览

当前项目为前后端分离结构：

- `backend/`：Python 后端，负责实时监听、DuckDB 存储、空间查询、统计接口
- `frontend/`：Vue3 + Vite 前端，负责地图展示、列表展示和 GIS 交互

当前主链路为：

`listener.py -> database.py -> service.py -> api.py -> Vue 前端`

## 3. 当前运行方式

### 3.1 后端启动

进入目录：

```powershell
cd e:\ljc\work\learning\earthquake\backend
```

如果使用 `uv`：

```powershell
uv sync
uv run main.py
```

如果使用 `pip`：

```powershell
pip install -e .
python main.py
```

后端默认监听：

- `http://127.0.0.1:5000`

说明：

- `main.py` 会同时启动 Flask API 和实时 WebSocket 监听
- 后端依赖 DuckDB 与 `spatial` 扩展
- 实时监听依赖外部地震 WebSocket 数据源联网可达

### 3.2 前端启动

进入目录：

```powershell
cd e:\ljc\work\learning\earthquake\frontend
```

项目包含 `pnpm-lock.yaml`，优先使用 `pnpm`：

```powershell
pnpm install
pnpm dev
```

前端默认访问地址：

- `http://localhost:5173`

说明：

- 前端开发服务器通过 Vite 代理把 `/earthquakes` 和 `/stats` 请求转发到后端 `5000`

## 4. 当前数据与存储说明

当前主要数据库文件位于：

- `backend/earthquakes.duckdb`

当前核心事实表为：

- `earthquakes`

现有稳定使用的字段包括：

- `unid`
- `time`
- `latitude`
- `longitude`
- `depth`
- `magnitude`
- `region`
- `geom`
- `created_at`

当前系统以近 48 小时实时数据展示为主。

## 5. 当前已有功能

### 5.1 实时采集能力

- 基于 Tornado 的 WebSocket 客户端连接 EMSC 实时地震流
- 自动解析事件字段
- 基于 `unid` 去重写入 DuckDB
- 支持断线重连和定时重启连接

### 5.2 后端查询与分析能力

- 最近地震事件查询
- GeoJSON 输出
- 半径范围查询
- 矩形范围查询
- 最近邻查询
- 缓冲区分析
- 空间叠加分析
- 格网聚类统计
- 区域统计
- 震级分布统计
- 小时分布统计
- 时间轴回放数据查询

### 5.3 前端展示能力

- 世界地图地震散点展示
- 热力图模式切换
- 板块边界显示
- 时间轴播放
- 地震列表与点击定位
- 聚类视图
- 缓冲区显示
- 最近邻查询模式

## 6. 当前关键接口

以下为当前已存在、适合基线验证的主要接口。

### 6.1 事件接口

- `GET /earthquakes`
- `GET /earthquakes.geojson`
- `GET /earthquakes/near`
- `GET /earthquakes/buffer`
- `GET /earthquakes/overlay`
- `GET /earthquakes/nearest`
- `GET /earthquakes/timeline`

### 6.2 统计接口

- `GET /stats/cluster`
- `GET /stats/region`
- `GET /stats/magnitude-distribution`
- `GET /stats/hourly-distribution`

## 7. 推荐的基线验证顺序

在开始竞赛化改造前，建议按以下顺序验证当前系统。

### 7.1 服务启动验证

1. 启动后端，确认 `5000` 端口服务正常
2. 启动前端，确认 `5173` 页面可访问

### 7.2 页面验证

1. 打开前端页面
2. 确认地图正常加载
3. 确认地震散点或热力图可以显示
4. 确认侧边栏地震列表正常展示

### 7.3 接口验证

建议至少验证以下两个接口：

```text
GET http://127.0.0.1:5000/earthquakes
GET http://127.0.0.1:5000/stats/cluster
```

通过标准：

- 接口返回 JSON
- 不出现服务端报错
- 返回结果非空或结构合理

## 8. 当前系统边界

当前系统已经具备“实时采集 - 存储 - 查询 - GIS 展示”的完整主干，但仍存在以下边界：

- 以近 48 小时实时态势为主
- 缺少稳定的历史数据底座
- 缺少事件特征层
- 缺少风险评分层
- 缺少辅助决策输出

因此，后续改造应以补分析层为主，而不是继续堆叠展示层。

## 9. 第一阶段改造前的结论

截至本文档编写时，项目基线判断如下：

- 当前项目具备稳定主链路
- 当前项目适合在现有架构上增量改造
- 后续开发应避免推翻现有后端查询链路和前端地图组件
- 最适合优先补齐的方向是历史数据、特征构建、风险评估与辅助决策接口

# 当前系统运行说明

## 1. 文档用途

本文档用于记录当前项目在 MVP 阶段的可运行状态，帮助后续开发者快速理解：

- 项目如何启动
- 前后端分别运行在什么端口
- 当前已有哪些能力
- 当前可用于验证 MVP 链路是否正常的任务和接口有哪些

## 2. 当前项目结构概览

当前项目为前后端分离结构：

- `backend/`：Python 后端，负责实时监听、DuckDB 存储、空间查询、统计接口
- `frontend/`：Vue3 + Vite 前端，负责地图展示、列表展示和 GIS 交互

当前主链路为：

`listener.py -> database.py -> service.py -> api.py -> Vue 前端`

当前分析链路为：

`import_history.py -> feature_builder.py / refresh_features.py -> risk_scorer.py / score_risk.py -> 风险接口 -> 前端风险面板`

## 3. 当前运行方式

### 3.1 后端启动

进入目录：

```powershell
cd e:\ljc\work\learning\earthquake\backend
```

如果使用 `uv`：

```powershell
uv sync
uv run main.py
```

如果使用 `pip`：

```powershell
pip install -e .
python main.py
```

后端默认监听：

- `http://127.0.0.1:5000`

说明：

- `main.py` 会同时启动 Flask API 和实时 WebSocket 监听
- 后端依赖 DuckDB 与 `spatial` 扩展
- 实时监听依赖外部地震 WebSocket 数据源联网可达

### 3.2 前端启动

进入目录：

```powershell
cd e:\ljc\work\learning\earthquake\frontend
```

项目包含 `pnpm-lock.yaml`，优先使用 `pnpm`：

```powershell
pnpm install
pnpm dev
```

前端默认访问地址：

- `http://localhost:5173`

说明：

- 前端开发服务器通过 Vite 代理把 `/earthquakes` 和 `/stats` 请求转发到后端 `5000`

## 4. 当前数据与存储说明

当前主要数据库文件位于：

- `backend/earthquakes.duckdb`

当前核心表包括：

- `earthquakes`
- `earthquake_features`
- `earthquake_risk_scores`

`earthquakes` 现有稳定使用的字段包括：

- `unid`
- `time`
- `latitude`
- `longitude`
- `depth`
- `magnitude`
- `region`
- `geom`
- `source`
- `source_event_id`
- `is_realtime`
- `ingest_time`
- `created_at`

`earthquake_features` 当前核心字段包括：

- `event_unid`
- `event_time`
- `region`
- `magnitude`
- `depth`
- `recent_window_hours`
- `recent_region_event_count`
- `recent_region_avg_magnitude`
- `historical_baseline_years`
- `historical_region_event_count`
- `historical_avg_daily_count`
- `historical_daily_count_stddev`
- `anomaly_score`
- `feature_version`
- `refreshed_at`

`earthquake_risk_scores` 当前核心字段包括：

- `event_unid`
- `event_time`
- `region`
- `risk_score`
- `risk_level`
- `magnitude_component`
- `depth_component`
- `activity_component`
- `anomaly_component`
- `explanation`
- `score_version`
- `scored_at`

## 5. 当前已有功能

### 5.1 实时采集能力

- 基于 Tornado 的 WebSocket 客户端连接 EMSC 实时地震流
- 自动解析事件字段
- 基于 `unid` 去重写入 DuckDB
- 支持断线重连和定时重启连接

### 5.2 后端查询与分析能力

- 最近地震事件查询
- GeoJSON 输出
- 半径范围查询
- 矩形范围查询
- 最近邻查询
- 缓冲区分析
- 空间叠加分析
- 格网聚类统计
- 区域统计
- 震级分布统计
- 小时分布统计
- 时间轴回放数据查询
- 历史时间分布统计
- 历史区域分布统计
- 事件特征刷新
- 规则型风险评分
- 风险排行查询
- 单事件风险详情查询

### 5.3 前端展示能力

- 世界地图地震散点展示
- 热力图模式切换
- 板块边界显示
- 时间轴播放
- 地震列表与点击定位
- 聚类视图
- 缓冲区显示
- 最近邻查询模式
- 风险排行面板
- 单事件风险详情抽屉

## 6. 当前关键接口

以下为当前已存在、适合 MVP 验证的主要接口。

### 6.1 事件接口

- `GET /earthquakes`
- `GET /earthquakes.geojson`
- `GET /earthquakes/near`
- `GET /earthquakes/buffer`
- `GET /earthquakes/overlay`
- `GET /earthquakes/nearest`
- `GET /earthquakes/timeline`

### 6.2 统计接口

- `GET /stats/cluster`
- `GET /stats/region`
- `GET /stats/magnitude-distribution`
- `GET /stats/hourly-distribution`
- `GET /stats/history/timeline`
- `GET /stats/history/region_dist`

### 6.3 风险接口

- `GET /risk/ranking`
- `GET /risk/events/<event_unid>`

## 7. MVP 推荐执行顺序

建议按以下顺序完成从近空数据库到风险面板展示的 MVP 流程。

### 7.1 后端初始化

```powershell
cd e:\ljc\work\learning\earthquake\backend
uv run init_db.py
```

### 7.2 导入历史数据

默认推荐使用官方 USGS 历史接口：

```powershell
uv run python jobs\import_history.py --source-type usgs --years 1 --usgs-min-magnitude 4.5
```

如果需要回退到仓库内置样例 CSV：

```powershell
uv run python jobs\import_history.py --source-type csv --source-path earthquakes.csv --years 3
```

### 7.3 刷新事件特征

```powershell
uv run python jobs\refresh_features.py --hours 30000 --limit 20 --baseline-years 3
```

### 7.4 生成风险评分

```powershell
uv run python jobs\score_risk.py --limit 20
```

### 7.5 启动后端与前端

后端：

```powershell
uv run main.py
```

前端：

```powershell
cd e:\ljc\work\learning\earthquake\frontend
pnpm dev
```

## 8. 推荐验证顺序

建议按以下顺序验证 MVP 闭环。

### 8.1 服务启动验证

1. 启动后端，确认 `5000` 端口服务正常
2. 启动前端，确认 `5173` 页面可访问

### 8.2 页面验证

1. 打开前端页面
2. 确认地图正常加载
3. 确认地震散点或热力图可以显示
4. 确认侧边栏地震列表正常展示
5. 确认风险面板可以展示排行
6. 点击风险事件后，确认可看到详情抽屉

### 8.3 接口验证

建议至少验证以下四个接口：

```text
GET http://127.0.0.1:5000/earthquakes
GET http://127.0.0.1:5000/stats/cluster
GET http://127.0.0.1:5000/stats/history/timeline?years=3&bucket=month
GET http://127.0.0.1:5000/risk/ranking?hours=30000&limit=5&min_risk_level=low
```

通过标准：

- 接口返回 JSON
- 不出现服务端报错
- 返回结果非空或结构合理

## 9. 当前系统边界

当前系统已经具备“历史导入 - 特征构建 - 风险评分 - 风险展示”的最小闭环，但仍存在以下边界：

- 当前前端风险面板仍是最小实现
- 历史图表尚未接入前端
- 尚未做人口暴露评估
- 尚未做区域异常识别
- 尚未做自动报告输出

因此，下一轮应优先做联调细化与历史图表补齐，而不是重做整体视觉结构。

## 10. 当前阶段结论

截至本文档编写时，MVP 状态判断如下：

- 当前项目已经具备稳定主链路和最小风险闭环
- 当前项目适合继续在现有架构上增量扩展
- 原有地图与 GIS 主链路可以与新增风险链路并行共存
- 后续最适合优先补齐的方向是历史图表联动、回归验证和展示材料整理

# 基于 MVP 的竞赛化改造实施计划

## 1. 目标与范围

本计划用于指导 AI 开发者在现有地震项目基础上，按最小可行产品（MVP）路线完成“从实时展示系统到辅助决策系统”的第一轮改造。

本计划遵循以下原则：

- 不推翻现有链路，保留 `listener.py -> database.py -> service.py -> api.py -> 前端组件` 主架构。
- 先跑通“历史数据 + 风险评估 + 前端查看”的核心闭环，再逐步扩展暴露分析、热点识别和自动报告。
- 每一步都必须足够小、可独立完成、可独立验证。
- 每一步都必须包含测试或验收动作。
- 本文件只提供清晰指令，不提供代码实现。

MVP 核心闭环定义如下：

1. 可以导入一批历史地震数据进入统一数据层。
2. 可以基于统一数据层为事件计算基础特征和风险评分。
3. 可以通过接口查询高风险事件列表和单事件评估结果。
4. 前端可以展示高风险事件列表并查看事件的基础评估信息。

## 2. 实施顺序

严格按以下顺序执行：

1. 基线整理
2. 统一配置
3. 统一事件表
4. 历史数据导入
5. 历史统计接口
6. 特征构建
7. 风险评分
8. 风险接口
9. 前端风险面板
10. 联调与回归测试
11. 第二阶段扩展准备

## 3. AI 开发者执行约束

执行每一步时必须遵守以下要求：

- 只修改当前步骤明确涉及的文件，避免大面积重构。
- 先阅读相关文件，再动手修改。
- 每一步完成后必须运行该步骤要求的测试。
- 若测试失败，必须先修复当前步骤，不得带病进入下一步。
- 每一步完成后，必须记录“修改了哪些文件、增加了什么能力、如何验证通过”。
- 新增能力要优先通过后端接口暴露，不要优先做复杂前端视觉优化。
- 所有新字段、新接口、新模块命名必须保持直观一致，避免缩写混乱。

## 4. MVP 分步指令

### 步骤 0：建立实施基线说明

目标：
确认当前系统可启动、当前接口可访问、当前前后端链路正常，为后续改造提供基线。

给 AI 开发者的指令：

1. 阅读后端入口、数据库访问、业务层、API 层和前端主地图组件。
2. 梳理当前已有接口、端口、启动方式和关键数据表。
3. 在当前目录补充一份简短运行说明，记录前后端启动方法和当前已有能力。
4. 不改变现有业务逻辑，只补充说明性文档或必要注释。

本步骤验证测试：

- 能成功启动后端服务。
- 能成功启动前端服务。
- 在浏览器中打开页面后，地图和基础图层可以正常显示。
- 随机调用一个现有地震接口和一个现有统计接口，确认返回非报错结果。

完成标准：

- 项目当前可运行状态被确认。
- 后续开发者可以依靠说明快速复现环境。

### 步骤 1：新增统一配置入口

目标：
避免新增逻辑继续分散硬编码，为后续历史导入和风险评分做准备。

给 AI 开发者的指令：

1. 新增统一配置文件，集中管理数据库路径、默认时间窗口、历史导入范围、风险评分权重、默认查询条数等参数。
2. 让后续新增模块优先从统一配置读取参数，不要在新文件中写死常量。
3. 仅在必要处接入配置，不要一次性重构所有旧文件。
4. 为配置项添加清晰中文注释或说明文字，便于后续写报告。

本步骤验证测试：

- 项目仍可正常启动。
- 修改配置中的默认时间窗口后，至少一个相关接口的默认行为发生可预期变化。
- 未配置项不会导致程序启动失败。

完成标准：

- 新增功能都能从统一配置读取核心参数。
- 现有系统行为不被破坏。

### 步骤 2：建立竞赛化扩展目录

目标：
为分析层扩展预留清晰结构，但不引入无用复杂度。

给 AI 开发者的指令：

1. 在后端新增分析、任务、报告相关目录。
2. 为每个目录补充最小化说明文件，明确用途。
3. 不要提前创建大量空模块，只创建 MVP 近期会用到的文件。
4. 保持目录命名简洁统一，后续所有新增分析逻辑都优先进入这些目录。

本步骤验证测试：

- 新目录创建后，后端服务仍可正常启动。
- 新目录结构能被后续模块直接引用，不出现导入路径错误。

完成标准：

- 后端扩展目录清晰可用。
- 没有引入无意义模板文件。

### 步骤 3：设计统一事件表最小扩展

目标：
让实时数据与历史数据可以进入同一数据层。

给 AI 开发者的指令：

1. 审查现有地震表结构和写入逻辑。
2. 在不破坏旧查询的前提下，为统一事件表补充最小必要字段，至少覆盖数据来源标识、源事件标识、是否实时、数据入库时间。
3. 优先兼容现有实时写入逻辑，避免影响当前监听器。
4. 若需要迁移或初始化逻辑，新增单独任务脚本处理，不要把迁移逻辑塞进运行时主链路。
5. 为新增字段补充数据字典说明。

本步骤验证测试：

- 实时监听链路仍能正常写入事件。
- 旧接口查询结果不出现结构性报错。
- 通过数据库检查可确认新增字段存在，且新写入事件在这些字段上有合理默认值。

完成标准：

- 实时与历史事件拥有统一存储基础。
- 原有查询与展示链路不受破坏。

### 步骤 4：实现历史数据导入任务

目标：
让系统具备导入历史地震数据的最小能力。

给 AI 开发者的指令：

1. 新建历史数据导入任务文件。
2. 先支持一种最稳定、最容易落地的历史数据来源，不要一开始接多个来源。
3. 导入流程必须包含拉取、字段映射、时间标准化、去重写入四个环节。
4. 导入任务必须支持限定时间范围，至少能导入近一年数据。
5. 导入任务必须支持重复执行，重复执行后不能产生明显重复数据。
6. 输出简洁可读的导入统计信息，如拉取条数、新增条数、跳过条数、失败条数。

本步骤验证测试：

- 首次导入后，数据库中的历史事件数量显著增加。
- 再次执行相同导入任务，不会产生大规模重复数据。
- 随机抽取若干历史事件，检查时间、经纬度、震级、区域字段是否完整。
- 导入完成后，现有实时监听功能仍可继续工作。

完成标准：

- 数据库中同时存在实时与历史事件。
- 历史导入具备最小幂等性。

### 步骤 5：实现事件标准化清洗任务

目标：
把历史数据和实时数据统一成可分析的数据质量标准。

给 AI 开发者的指令：

1. 新建标准化任务文件，专门处理时间格式、区域名称、经纬度合法性、缺失值兜底、重复事件识别规则。
2. 将标准化规则写成独立可复用逻辑，避免散落在多个接口中。
3. 对明显异常数据进行标记或跳过，不要静默吞掉问题数据。
4. 标准化结果要能服务后续特征构建，不只满足展示。

本步骤验证测试：

- 对一批历史数据执行标准化后，时间格式保持一致。
- 不合法经纬度不会进入有效分析结果。
- 区域字段的空值率或脏数据比例明显下降。
- 同一批数据重复标准化后，结果保持稳定。

完成标准：

- 数据质量达到可进行统计与评分的水平。

### 步骤 6：新增历史统计接口

目标：
先打通最基础的历史视角，为后续异常分析提供观察入口。

给 AI 开发者的指令：

1. 新增历史时间分布接口，支持按月或按日聚合查看事件数变化。
2. 新增历史区域分布接口，支持查看主要区域的事件分布。
3. 接口参数保持简单，优先支持时间范围、聚合粒度、返回条数限制。
4. 保持返回格式适合前端图表直接消费。
5. 不要在这个阶段加入复杂筛选器。

本步骤验证测试：

- 调用历史时间分布接口，可返回连续时间序列数据。
- 调用历史区域分布接口，可返回非空区域统计。
- 在导入数据量变化后，统计结果会随之变化，说明接口不是写死结果。

完成标准：

- 系统具备基础历史统计观察能力。

### 步骤 7：建立事件特征表或特征缓存层

目标：
为风险评分准备结构化输入。

给 AI 开发者的指令：

1. 设计最小特征集合，只保留能支撑第一版风险评分的字段。
2. 至少包含震级、深度、近期局部活跃度、历史基线均值、历史波动程度、异常分数等特征。
3. 特征结果应与事件唯一标识关联。
4. 先做可离线刷新或手动刷新版本，不要一开始追求实时自动增量更新。
5. 明确哪些特征来自当前窗口，哪些来自历史基线。

本步骤验证测试：

- 对一批事件刷新特征后，可查询到对应特征结果。
- 至少抽查三个不同区域的事件，确认特征值存在差异，而不是统一默认值。
- 同一数据集重复刷新后，特征结果保持一致。

完成标准：

- 风险评分所需的第一版特征已经准备完成。

### 步骤 8：实现特征构建任务

目标：
让系统可以批量生成事件特征。

给 AI 开发者的指令：

1. 新建特征刷新任务文件和分析文件。
2. 任务支持指定时间窗口，只刷新近期重点事件，避免一开始全库重算。
3. 输出刷新统计结果，如处理事件数、成功数、失败数、耗时。
4. 将复杂计算逻辑放在分析模块中，任务文件只负责调度。
5. 如果现有数据库查询性能不足，优先优化查询范围，不要提前引入复杂框架。

本步骤验证测试：

- 在指定时间窗口内执行刷新后，相关事件都有对应特征。
- 不在窗口内的事件不会被错误重算。
- 对同一窗口重复刷新两次，结果稳定。

完成标准：

- 特征构建流程可重复执行并稳定产出结果。

### 步骤 9：实现第一版规则型风险评分

目标：
让系统从“描述事件”进入“评估事件”。

给 AI 开发者的指令：

1. 新建风险评分模块。
2. 第一版仅采用可解释的规则加权评分，不使用复杂模型。
3. 评分至少考虑震级、深度、近期活跃度、历史异常程度四部分。
4. 输出必须包含综合分数、风险等级、评分解释。
5. 评分解释必须是面向答辩和报告可直接复用的自然语言摘要。
6. 权重必须来自统一配置，便于后续调优和做消融实验。

本步骤验证测试：

- 对同一组事件执行评分后，可得到完整的分数、等级和解释。
- 高震级且异常程度更高的事件，综合分通常应高于普通小震事件。
- 修改某一权重配置后，评分结果会发生合理变化。
- 至少人工抽查五个事件，确认解释与分数逻辑一致。

完成标准：

- 风险评分逻辑稳定、可解释、可调参。

### 步骤 10：新增风险查询接口

目标：
把评分能力输出给前端和报告模块使用。

给 AI 开发者的指令：

1. 新增高风险事件排行接口。
2. 新增单事件评估详情接口。
3. 高风险排行接口至少支持时间窗口、返回条数、最小风险等级筛选。
4. 单事件详情接口至少返回基础信息、特征摘要、风险评分、风险解释。
5. 接口命名和返回结构保持清晰，避免前端额外做复杂拼装。

本步骤验证测试：

- 调用高风险排行接口，可返回按风险排序的事件列表。
- 调用单事件评估接口，可返回指定事件的完整评估信息。
- 不存在的事件标识会返回清晰错误，而不是服务崩溃。
- 空数据场景下接口返回结构仍然稳定。

完成标准：

- 前端已经具备消费风险结果的后端入口。

### 步骤 11：前端新增最小风险面板

目标：
以最小工作量把后端风险结果展示出来，先形成闭环。

给 AI 开发者的指令：

1. 新增一个风险排行面板组件。
2. 面板先展示高风险事件列表，不做复杂动画和视觉重构。
3. 列表中至少显示时间、区域、震级、风险等级、风险得分。
4. 点击某个事件后，可查看该事件的评估摘要。
5. 尽量复用现有侧边栏、弹窗或详情展示机制，不新造大块页面结构。
6. 优先保证信息清楚可读，再考虑样式美化。

本步骤验证测试：

- 页面加载后可以成功请求风险排行接口。
- 风险列表能稳定渲染，不因单条数据缺字段而整体报错。
- 点击列表项后，能看到单事件评估信息。
- 在后端调整时间窗口或数据后，前端内容会同步变化。

完成标准：

- 用户可以在前端直接看到“哪些事件更值得关注”。

### 步骤 12：前端增加历史基础图表

目标：
补齐历史基线的最小可视化支持。

给 AI 开发者的指令：

1. 在现有侧边栏或独立区域增加历史时间分布图。
2. 增加历史区域分布图。
3. 保持交互简单，不做复杂联动。
4. 图表命名、图例和说明文本要偏研究表达，而不是只做炫技展示。
5. 若现有页面空间不足，优先采用可折叠面板。

本步骤验证测试：

- 两个历史图表都能成功显示。
- 改变统计时间范围后，图表数据随之变化。
- 图表接口异常时，页面能显示友好提示而不是空白崩溃。

完成标准：

- 前端已能同时展示实时态势与历史基线。

### 步骤 13：完成 MVP 联调回归

目标：
确认 MVP 核心闭环完整可演示。

给 AI 开发者的指令：

1. 从空白或近空数据库开始，按顺序执行初始化、历史导入、特征刷新、风险评分、前后端联调。
2. 用一组固定时间窗口进行全流程演示验证。
3. 检查新增能力是否影响已有地图、热力图、时间轴、缓冲区、最近邻等旧功能。
4. 补充简短的接口说明和数据字典。

本步骤验证测试：

- 历史导入成功后，可查到历史统计结果。
- 特征刷新成功后，可查到特征或评分结果。
- 风险排行接口返回非空结果。
- 前端风险面板可展示排行榜并查看详情。
- 原有核心接口仍然可用。
- 原有地图主功能未出现明显回归问题。

完成标准：

- MVP 已具备可答辩演示的最小闭环。

## 5. MVP 完成后的下一阶段扩展指令

以下内容不进入第一轮 MVP 主线，必须在 MVP 验收通过后再做。

### 扩展步骤 A：人口暴露评估

目标：
把事件强度进一步转化为潜在影响。

给 AI 开发者的指令：

1. 选取一种稳定可获得的人口栅格或人口分布数据源。
2. 建立人口数据表和事件暴露结果表。
3. 基于现有缓冲区能力，计算事件周边不同半径的人口暴露量。
4. 先支持少量固定半径，不要一开始做任意半径。
5. 将暴露结果接入事件评估详情，但第一步不要立即重构全部前端。

本步骤验证测试：

- 指定事件可得到不同半径的人口暴露结果。
- 半径增大时，暴露人口通常不减少。
- 暴露结果可被单事件评估接口稳定返回。

### 扩展步骤 B：区域异常识别

目标：
从单事件判断扩展到区域活跃度判断。

给 AI 开发者的指令：

1. 采用固定网格或固定区域作为最小实现，不做多套区域体系。
2. 计算当前窗口事件数、历史均值、历史标准差和异常分数。
3. 输出热点区域清单和地图图层数据。
4. 优先保证统计逻辑正确，再考虑前端可视化效果。

本步骤验证测试：

- 热点接口能返回区域级异常结果。
- 当前窗口明显活跃的区域，其异常分数高于普通区域。
- 前端可显示最小热点列表或异常图层。

### 扩展步骤 C：自动分析报告

目标：
增强作品竞赛展示与答辩支撑能力。

给 AI 开发者的指令：

1. 先实现单事件报告，不要一开始做复杂日报和批量导出。
2. 报告内容至少包含事件概况、风险评分、评分原因、历史背景、暴露结果。
3. 输出格式优先考虑文本或结构化数据，后续再扩展为更精美文档。
4. 报告内容必须和接口返回保持一致，避免两套口径。

本步骤验证测试：

- 指定事件可以生成完整报告内容。
- 报告中的评分、区域、时间等关键字段与接口一致。
- 缺少暴露数据时，报告仍可生成降级版本。

## 6. 每一步完成后的统一记录模板

要求 AI 开发者在每一步结束后，必须补充以下记录：

- 本步目标是否完成
- 修改了哪些文件
- 新增了哪些接口、任务或字段
- 执行了哪些测试
- 测试结果是否通过
- 是否影响现有功能
- 下一步建议做什么

## 7. MVP 验收标准

当以下条件全部满足时，视为 MVP 完成：

- 系统可同时管理实时与历史地震数据。
- 可查询历史时间分布和区域分布。
- 可批量生成事件特征。
- 可对事件进行基础风险评分。
- 可通过接口查看高风险事件排行和单事件评估。
- 前端可展示高风险事件列表并查看基础评估信息。
- 原有地图与核心查询功能未明显退化。

## 8. 开发节奏建议

建议采用以下最小节奏：

- 第 1 轮只做到步骤 13，不展开人口暴露和自动报告。
- 第 2 轮再做人口暴露评估，并将其并入风险评分。
- 第 3 轮再做区域异常识别。
- 第 4 轮补自动报告、实验整理、研究报告材料。

如果时间紧张，必须优先保证以下链路：

- 历史导入
- 特征构建
- 风险评分
- 风险排行前端展示

这四项跑通后，作品就已经从“实时展示”进入“分析与辅助决策”方向，具备竞赛化升级的核心价值。
